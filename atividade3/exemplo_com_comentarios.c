#include <stdio.h>

int main() {
    int initial = 10; // valor inicial
    int rate = 5;
    /*
       O comentario de bloco deve ser removido
       antes da troca de linguagem.
    */
    int position = initial + rate * 60;

    if (position == 310) {
        printf("ok\n");
    }

    return 0;
}
